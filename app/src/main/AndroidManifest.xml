<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.VIBRATE" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyApplication">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyApplication">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- Realtime Home Screen Sync Widget Receiver -->
        <receiver android:name=".surjomukhi.widget.SurjomukhiWidgetProvider"
            android:exported="true"
            android:label="🌻 Surjomukhi Sync Widget">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.example.surjomukhi.ACTION_PING" />
                <action android:name="com.example.surjomukhi.ACTION_SET_FREE" />
                <action android:name="com.example.surjomukhi.ACTION_SET_STUDY" />
                <action android:name="com.example.surjomukhi.ACTION_SET_SLEEP" />
                <action android:name="com.example.surjomukhi.ACTION_SET_BUSY" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/surjomukhi_widget_info" />
        </receiver>

    </application>

</manifest>
